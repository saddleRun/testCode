build.sh

alias build="cd web && gulp build-non-versioned && cd .."
alias buildDev="cd web && gulp dev-clean-hint && cd .."
alias buildA="cd web && gulp rebuild && cd .."
alias eslint="cd web && gulp eslint-check && cd .."
alias jshint="cd web && gulp jshint-check && cd .."
alias buildR="cd web && npm run build-dev && cd .."
alias buildC='mvn clean package -Dpmd.skip=true -DskipTests -Pback-end-dev -pl core,web'
alias buildD='mvn clean package -Dpmd.skip=true -DskipTests -Pback-end-dev -pl core,document-server'
alias buildW='mvn clean package -DskipTests -Pback-end-dev'
alias buildwC='mvn clean package -Dmaven.test.skip=true -pl core,web'
alias chrometest="gulp unit-test --browser --chrome_headless"
alias install="mvn -T1C clean install -DskipTests -DskipRest -l ~/repos/pack.txt"
alias pack="mvn -T1C clean package -U -DskipTests -DskipRest -l ~/repos/packC.txt"
alias packC="mvn -T1C clean package -DskipTests"
alias comp="mvn -T1C clean compile"
alias installU="mvn -T1C clean install -U -DskipTests -DskipRest"
alias installCU="mvn -T1C clean install -U -DskipTests"
alias install2='mvn -T1C clean install  -Dmaven.test.skip=true -DskipRest'
alias karma='node_modules/.bin/karma start karma.conf.js --no-single-run'
alias tests='mvn -Dtest=UnmappedFieldsBuilderTest test -l ~/repos/test.txt'
alias httpT='mvn verify -pl httpTest -Dexec.skip -l ~/repos/httpT.txt'
alias reactT='npm run test-jest -t SearchButton.spec.tsx'
alias reactTA='npm run test-jest'
alias purge='mvn dependency:purge-local-repository'
alias mlist='mvn dependency:list'
alias idea='/home/haj25/IntelliJ/idea-IU-233.14808.21/bin/idea.sh > /dev/null 2>&1 &'
alias code='/usr/bin/code .. > /dev/null 2>&1 &'
alias e2e="./script/run_e2e_tests.sh -r all_scenarios -L https://stn-dev.cas.org/ -T '-t @TestId_320'"
alias e2eFolder="./script/run_e2e_tests.sh -L http://yourVM-desktop.planaria.cloud.cas.org/ -f features/scenarios/file_wizard"
alias e2eFeature="./script/run_e2e_tests.sh -L http://haj25-desktop.planaria.cloud.cas.org/ -f features/scenarios/alerts/alerts_page.feature"
alias controllerTest="mvn test -Dtest=ReportRestControllerTest -Dpmd.skip=true -Pback-end-dev -pl web -l ~/repos/build.txt"

====


docker.sh
alias start='docker-compose build  && ../scripts/minimal_local_startup.sh'
alias stopMCApp='../scripts/shutdown.sh && docker-compose down'
alias stop='../scripts/stack_shutdown.sh  && docker-compose down'
alias jLog='docker logs -f docker-compose_jetty_1 --since 1s'
alias rmI='docker rmi -f $(docker images -q)'
alias rmC='docker rm $(docker ps -aq)'
alias stopJ='docker stop docker-compose_jetty_1'
alias startJ='docker start docker-compose_jetty_1'
alias startRJ='docker start docker-compose_report-server_1 docker-compose_jetty_1'
alias startAJ='docker start docker-compose_apache_1 docker-compose_jetty_1'
alias stopAJ='docker stop docker-compose_apache_1 docker-compose_jetty_1'
alias stopRJ='docker stop docker-compose_report-server_1 docker-compose_jetty_1'
alias dockerNames='docker ps --format {{.Names}}'
alias dockerIDs='docker ps --format {{.ID}}'
alias dockerImg='docker ps --format {{.Image}}'


========git.sh
// gitbash
TERM=xterm     // to fix back space in Git bash
TERM=xterm-256color
cd C:/CASNC/repos/repoDIR
alias fetchR='git fetch; git pull --rebase'


alias pushBR='git push origin HEAD:refs/for/feature/parsed-data'
alias fetchR='git fetch; git pull --rebase'
alias gitlog='git --no-pager log --graph --all --oneline > git-history.txt'
echo 'git checkout -b PLAN-13674 origin/feature/parsed-data'
echo 'git checkout -b PLAN-15982 v1.0.2"
# git fetch --all --tags // Fetch the latest tags and branches from the remote
# git tag // Check available tags
# git checkout -b new-branch-name tag-name // Create a branch from a local tag
# git checkout -b new-branch-name origin/tags/tag-name // Create a branch from a remote tag

git config --list     // list all the configuration

$ git config --global core.editor "atom"
$ git config --global core.editor "nano"
$ git config --global core.editor "kate"
git config --list --show-origin  // You will see which config is set where.
git config --global -e  // open .gitconfig  will the editor set

// git lab
git push origin MC-70
git push origin MC-70 --force // in case some one made a change to the branch
git fetch --all  // To get all the changes from all the branches
git fetch --all --prune  // to clean up some of the branches that no longer exist in the remote repository
git push --force // allows overwriting remote branches
git fetch --force (or git pull --force) // allows overwriting local branches
git restore .         // For all unstaged files in current working directory
git restore path/to/file/to/revert // For a specific file


--- Test Ed25519 Support on Ubuntu 
To check if your system supports the Ed25519 algorithm, you can run the following command in your terminal:

  $ssh -Q key // This command lists the key types supported by your SSH client. 

If you see ed25519 in the output, your system supports Ed25519 keys, and you can proceed to generate an 
Ed25519 SSH key pair. 
If ed25519 is not listed, you may need to use a different algorithm (like RSA) or update your SSH client 
to a version that supports Ed25519.

  $ssh -V // to check the version of ssh client, ed25519 is supported in OpenSSH 6.5 and later versions.

-----2. Generate a test Ed25519 key pair 
You can generate a test Ed25519 key pair using the ssh-keygen command. 
Run the following command in your terminal:
  $ssh-keygen -t ed25519 -C "saddleRun"

This command will create a new Ed25519 key pair. You will be prompted to specify a file to save the key
(default is ~/.ssh/id_ed25519) and to enter a passphrase for added security (optional but recommended).

----3. Test connectivity with the new key 
To test if the new Ed25519 key works for SSH authentication, you can use the ssh command to connect to a 
server that accepts your public key.
  $ssh -i ~/.ssh/id_ed25519 user@hostname
Replace user@hostname with the appropriate username and server address. If the connection is successful, 
it indicates that your Ed25519 key is working correctly for SSH authentication.

---- To test if the key is offered during connection
You can use the ssh command with the -v (verbose) option to see detailed output of the SSH connection process, 
including which keys are being offered for authentication:
  $ssh -v -i ~/.ssh/id_ed25519 user@hostname
This will show you the debug output, and you should see lines indicating that the Ed25519 key is being 
offered for authentication.

---- To force the connection to use only the Ed25519 key:
You can use the -o option to specify SSH configuration options directly in the command line. 
To force the SSH client to use only the Ed25519 key, you can run:
  $ssh -o PubkeyAuthentication=yes -o PreferredAuthentications=publickey -o IdentityFile=~/.ssh/id_ed25519 user@hostname

  ssh -i ~/.ssh/id_ed25519 user@hostname // to connect using the Ed25519 key

========
josh $ ssh-keygen -t ed25519 -C "your_private_Key"
Generating public/private ed25519 key pair.
Enter file in which to save the key (/home/josh/.ssh/id_ed25519): 
Enter passphrase (empty for no passphrase): 
Enter same passphrase again: 
Your identification has been saved in /home/josh/.ssh/id_ed25519
Your public key has been saved in /home/josh/.ssh/id_ed25519.pub
The key fingerprint is:
SHA256:+gU/bw85ToEyxwYmxPcz/MlUWi4SEfJkJXVkfo8CxoU .....
The key's randomart image is:
+--[ED25519 256]--+
|     .. . **+.+  |
|     .. .*Eo +o  |
|      ..oo=. =. .|
|       o +*o+ .o.|
|        S =Boo. .|
|       . B  ++   |
|      .   + =    |
|       . . =.o   |
|        .  .o..  |
+----[SHA256]-----+
josh $ cat ~/.ssh/id_ed25519.pub
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIMBT2x...

// get the public ssh key
$cat ~/.ssh/id_rsa.pub // to get the public key in linux and mac
$cat ~/.ssh/id_ed25519.pub

$clip < ~/.ssh/id_rsa.pub  // to copy the public key to clipboard in windows
$clip < ~/.ssh/id_ed25519.pub



How to create a Git branch from a tag
https://graphite.dev/guides/git-create-branch-from-tag

Step 1: Fetch the latest tags and branches from the remote
Before creating a new branch from a tag, especially a remote tag, you should update your local repository 
with all the tags from the remote repository.

$git fetch --all --tags
This command fetches all branches and tags from the remote, ensuring your repository is up to date.

Step 2: Check available tags
To view the list of tags in your repository, use:

$git tag
This will list all the tags in your repository, helping you decide from which tag you want to create a 
new branch.

Step 3: Create a branch from a local tag
If you have already fetched the tags from the remote or you are working with a local tag, you can create 
a new branch from it using the following command:

$git checkout -b new-branch-name tag-name
Replace new-branch-name with the desired name for your new branch, and tag-name with the name of the tag 
you want to branch from.

Step 4: Create a branch from a remote tag
If you want to create a branch directly from a remote tag (especially if the tag isn't present locally), you can do so by 
specifying the full tag reference:

$git checkout -b new-branch-name origin/tags/tag-name


Again, replace new-branch-name with your branch name and tag-name with the remote tag name.

Step 5: Push the new branch to remote
When you create a new branch locally in Git, it exists only on your local machine until you explicitly push it 
to a remote repository. Once you've pushed the branch to the remote repository, it becomes available to others 
who have access to that repository. 
Additionally, when you push a branch to a remote repository for the first time, Git automatically sets up a 
remote-tracking branch associated with that branch on the remote repository.

A remote-tracking branch is a local reference in your repository that keeps track of the state of a branch 
on a remote repository. It allows Git to know which commits have been added to the remote branch since the 
last time you fetched from or pushed to the remote repository.

To push your branch, run:

$git push origin new-branch-name
This command pushes the new branch to the remote repository (assuming 'origin' is your remote name).
